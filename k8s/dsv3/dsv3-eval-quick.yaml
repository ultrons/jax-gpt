# Quick 8-layer eval to verify weight loading + forward pass correctness.
# Then submit full 61-layer overnight.
---
apiVersion: jobset.x-k8s.io/v1alpha2
kind: JobSet
metadata:
  name: dsv3-eval-quick
  namespace: default
  labels:
    kueue.x-k8s.io/queue-name: multislice-queue
  annotations:
    alpha.jobset.sigs.k8s.io/exclusive-topology: cloud.google.com/gke-nodepool
spec:
  failurePolicy:
    maxRestarts: 0
  replicatedJobs:
    - name: slice
      replicas: 1
      template:
        spec:
          completions: 8
          parallelism: 8
          completionMode: Indexed
          backoffLimit: 0
          template:
            metadata:
              labels:
                app: dsv3-eval-quick
            spec:
              restartPolicy: Never
              nodeSelector:
                cloud.google.com/gke-tpu-accelerator: tpu7x
                cloud.google.com/gke-tpu-topology: 2x4x4
              volumes:
                - name: model-weights
                  persistentVolumeClaim:
                    claimName: ds-v3-weights-pvc
              containers:
                - name: eval
                  image: gcr.io/tpu-vm-gke-testing/mini-dsv3:v8-eval
                  imagePullPolicy: Always
                  command: ["python", "eval_logits.py"]
                  args:
                    - "--model_dir=/mnt/model/DeepSeek-V3"
                    - "--fsdp=64"
                    - "--ep=1"
                    - "--n_layers=8"
                  volumeMounts:
                    - name: model-weights
                      mountPath: /mnt/model
                      readOnly: true
                  env:
                    - name: PYTHONUNBUFFERED
                      value: "1"
                    - name: TRANSFORMERS_OFFLINE
                      value: "1"
                  resources:
                    limits:
                      google.com/tpu: "4"
